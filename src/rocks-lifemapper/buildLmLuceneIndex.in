#!/bin/bash

# Purpose: rebuild the Lifemapper species lucene index
# Usage: run as a cron job
#
# Based on distro's buildLmLuceneIndex.cron

# Set environment in case it is not already set
. /etc/profile.d/lifemapper.sh

# Build the index
/opt/python/bin/@PYTHONVER@ @LMHOME@/LmWebServer/src/lucene/lmLucene.py build

# Note: the output  of build can be piped to a file to check for success
# or check the files modification time in @LMWEB@/luceneIndex/
