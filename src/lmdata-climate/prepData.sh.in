#!/bin/bash

# Purpose: Fetch climate data named in CLIMATE_DATA  

create_climate_data () {
  echo "Fetch climate data"
  LMURL="http://lifemapper.org/dl"
  for i in @CLIMATE_DATA@; do
    curl -L "$LMURL/$i" -o ../lmdata-climate/$i.@TARBALL_POSTFIX@
  done 
}

### main ###
create_climate_data
